---

- validate:
    schema:
      type: object
      properties:
        build:
          type: string
          default: stable
          enum:
            - stable
            - edge
        tls_cert_url:
          type: string
          format: uri
        tls_key_url:
          type: string
          format: uri
        uninstall_previous_versions:
          type: boolean
          default: true
        user:
          type: string
          default: ubuntu
      required:
        - build
        - uninstall_previous_versions
        - user
    instance: "{{ docker }}"
  register: docker_validated

- set_fact:
    docker_v: "{{ docker_validated.result }}"

- block:
    - name: "Create /etc/docker"
      file:
        path: /etc/docker
        state: directory

    - name: "Download Docker TLS certificate"
      get_url:
        url: "{{ docker_v.tls_cert_url }}"
        dest: /etc/docker/tls_cert.pem
        force: yes
      when: "'tls_cert_url' in docker_v"

    - name: "Download Docker TLS key"
      get_url:
        url: "{{ docker_v.tls_key_url }}"
        dest: /etc/docker/tls_key.pem
        force: yes
      when: "'tls_key_url' in docker_v"

    - debug:
        var: docker_v

    - name: "Install Docker and Compose"
      import_role:
        name: angstwad.docker_ubuntu
      vars:
        daemon_json:
          iptables: yes
          ip-forward: yes
          tls: yes
          tlscert: "{{ ('tls_cert_url' in docker_v) | ternary('/etc/docker/tls_cert.pem', None) }}"
          tlskey: "{{ ('tls_key_url' in docker_v) | ternary('/etc/docker/tls_key.pem', None) }}"
          hosts:
            - tcp://0.0.0.0:2376
            - unix:///var/run/docker.sock
        docker_apt_channel: "{{ docker_v.build }}"
        docker_group_members: "{{ docker_v.user }}"
        uninstall_previous_docker_versions: "{{ docker_v.uninstall_previous_versions }}"

    - name: "Enable ip forwarding in the kernel"
      lineinfile:
        path: /etc/sysctl.conf
        regexp: net.ipv4.ip_forward
        line: net.ipv4.ip_forward=1
      notify: sysctl_reload
  become: yes

- name: "Reset the ssh connection to pick up docker group membership"
  import_role:
    name: jcheroske.common
  vars:
    common:
      command: controller_reset_connection
