galaxy_info:
  author: Jay Cheroske
  description: |
    A role that performs several docker-related functions,
    including installation and firewall configuration
  license: GPLv3
  min_ansible_version: 2.5
  github_branch: galaxy
  platforms:
    - name: Ubuntu
      versions:
        - xenial
        - bionic
  galaxy_tags:
    - docker
dependencies:
  - { role: jcheroske.ansible_role_common }
